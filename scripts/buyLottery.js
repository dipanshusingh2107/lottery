const hre = require("hardhat"); //optional needed if using node
require('dotenv').config();

main = async ()=> {
    
}


main()
.then(() => process.exit(0))
.catch(error => {
  console.error(error);
  process.exit(1);
});
